process {
    container = 'my-pipeline:arm64'
    executor = 'local'
    cpus = 2
}

docker {
    enabled = true
    runOptions = '--platform linux/arm64'
}

params {
    input = '/absolute/path/to/*.fastq.gz'        // 👈 Replace this
    outdir = '/absolute/path/to/output'           // 👈 Replace this
    genome_dir = '/absolute/path/to/genome/index' // 👈 Replace this
}
