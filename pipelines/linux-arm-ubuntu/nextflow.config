nextflow {
  enable.dsl = 2
}

process {
  executor = 'local'
  cpus = 2
}

params {
  input = '/home/ubuntu/data/*.fastq.gz'       // 👈 Replace this as needed
  outdir = '/home/ubuntu/output'               // 👈 Replace this as needed
  genome_dir = '/home/ubuntu/genome/STAR_index' // 👈 Replace this as needed
}
