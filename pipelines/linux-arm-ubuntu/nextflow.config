nextflow {
    enable.dsl = 2
}

process {
    executor = 'local'
    cpus = 2
}

params {
    // Adjust this path to match where your test data lives
    input = "pipelines/macos-intel-x86/nextflow-pixi/test_data/*.fasta"
    outdir = "results"
}
