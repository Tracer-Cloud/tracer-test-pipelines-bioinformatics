process {
    container = 'my-pipeline:arm64'
}
docker {
    enabled = true
    runOptions = '--platform linux/arm64'
}
params {
    input = '/path/to/*.fastq.gz'          // REPLACE
    outdir = '/path/to/output'             // REPLACE
    genome_dir = '/path/to/genome/index'   // REPLACE
}
