nextflow {
    enable.dsl = 2
}

process {
    executor = 'local'
    cpus = 2
}

params {
    input = "pipelines/linux-arm-ubuntu/test_data/*.fasta"
    outdir = "pipelines/linux-arm-ubuntu/results"
}

