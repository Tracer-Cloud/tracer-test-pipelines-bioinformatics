version: 6
environments:
  default:
    channels:
    - url: https://conda.anaconda.org/conda-forge/
    - url: https://conda.anaconda.org/bioconda/
    packages:
      linux-aarch64:
      # (Copy all package entries from the shared lock, but change platform to linux-aarch64)
      # For brevity, this is a placeholder. In practice, regenerate with pixi for ARM Ubuntu.
packages:
# (Copy all package entries from the shared lock, but change platform to linux-aarch64)
# For brevity, this is a placeholder. In practice, regenerate with pixi for ARM Ubuntu. 