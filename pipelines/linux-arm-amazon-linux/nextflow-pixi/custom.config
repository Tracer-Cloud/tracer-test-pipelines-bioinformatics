process.errorStrategy = 'ignore'

// Skip processes that require tools not available in this environment
process {
    withName: '.*BBMAP.*' {
        errorStrategy = 'ignore'
    }

    withName: '.*FQ_LINT.*' {
        errorStrategy = 'ignore'
    }
}

// Set resource limits for low-memory, low-CPU systems (optimized for ARM instances)
process {
    // Default resource limits - max 2 CPUs available
    cpus = 1
    memory = '512 MB'
    time = '1 h'

    // Override specific processes that typically need more resources
    withName: '.*GUNZIP.*' {
        cpus = 1
        memory = '512 MB'
    }

    withName: '.*CAT_FASTQ.*' {
        cpus = 1
        memory = '512 MB'
    }

    withName: '.*STAR.*' {
        cpus = 1
        memory = '512 MB'
    }

    withName: '.*HISAT2.*' {
        cpus = 1
        memory = '512 MB'
    }

    withName: '.*SALMON.*' {
        cpus = 1
        memory = '512 MB'
    }

    // Catch-all for any process that might still request too many resources
    withLabel: 'process_single' {
        cpus = 1
        memory = '512 MB'
    }

    withLabel: 'process_low' {
        cpus = 1
        memory = '512 MB'
    }

    withLabel: 'process_medium' {
        cpus = 1
        memory = '512 MB'
    }

    withLabel: 'process_high' {
        cpus = 1
        memory = '512 MB'
    }
} 