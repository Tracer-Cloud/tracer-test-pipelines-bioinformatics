bucketDir = 's3://tracer-nxf-work/work'

process {
    executor = 'awsbatch'
    queue = 'NextflowCPU'
    // Better for resuming failed workflows
    cache = 'deep'
}

aws {
    region = 'us-east-1'
    batch {
        cliPath = '/usr/local/aws-cli/v2/current/bin/aws'
    }
}
